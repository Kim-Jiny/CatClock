# 냥타이머 (가칭) — 기획서

## 한 줄 정의
맥 화면에 떠 있는, **고양이가 알람시계를 들고 있는 플로팅 타이머 위젯**.
퇴근 카운트다운이나 작업 타이머를 곁눈질로 귀엽게 확인.

## 타겟
맥 쓰는 직장인. "퇴근까지 얼마?" / "이 작업 몇 분째?"를 작업 방해 없이 보고 싶은 사람.

## 확정 사항
- **기술 스택**: 네이티브 Swift (SwiftUI + AppKit)
- **타이머 개수**: 1차는 1개. 단, 여러 개로 확장 가능한 구조로 설계
- **고양이 그래픽**: 정적 이미지 1~3종 (상태별 교체)
- 개발 환경: macOS 26.2, Xcode 26.2

---

## 핵심 기능

### 1. 플로팅 위젯
- 테두리 없는 투명 창 (NSPanel, non-activating)
- 마우스로 드래그해서 이동, 위치 기억(다음 실행 시 복원)
- 작은 형태: 고양이 + 시계 숫자만
- 표시 모드 (설정에서 전환):
  - **최상단 고정**: 다른 앱 위에 항상 보임 (window level = floating)
  - **데스크탑에만**: 바탕화면 레벨, 앱 켜면 가려짐 (desktop window level)

### 2. 타이머
- 모드 두 가지:
  - **퇴근 타이머**: 퇴근 시각 입력 → 남은 시간 카운트다운
  - **작업 타이머**: 분 단위 직접 설정 (예: 25분)
- 컨트롤: 시작 / 일시정지 / 리셋
- 종료 시 알림: 소리 + 화면 알림 + 고양이 상태 변화
- (확장 여지) 타이머 여러 개 / 프리셋 / 반복

### 3. 고양이 상태 표현 (정적 이미지 교체)
- 대기 (idle)
- 카운트다운 중 (running)
- 종료 / 시간 다 됨 (done)
- 이미지 에셋: **고양이 종류 N종 × 상태 3종**
- 임시 플레이스홀더로 시작, 실제 그림은 추후 교체

### 3-1. 고양이 종류 선택 (유저 설정)
- 유저가 위젯에 쓸 고양이 **종류(스킨)** 선택 가능
  - 예: 치즈냥 / 까만냥 / 삼색냥 / 턱시도냥 …
- 설정 화면에서 미리보기 + 선택, 선택값 저장(`UserDefaults`)
- 구조: `CatSkin` 모델 — id, 표시이름, 상태별 이미지 매핑
- 확장 여지: 시즌/한정 스킨, 유저 커스텀 이미지 업로드

### 4. 메뉴바 아이콘
- 상단 메뉴바에 상주 (NSStatusItem)
- 위젯 표시/숨김, 설정 열기, 종료

---

## 기술 설계 메모
- `NSPanel` + `.nonactivatingPanel`, `isOpaque=false`, 투명 배경, 그림자/테두리 없음
- 윈도우 레벨:
  - 최상단 = `.floating` (필요 시 `.screenSaver`)
  - 데스크탑 = `CGWindowLevelForKey(.desktopWindow)` 근처
- 드래그: `isMovableByWindowBackground` 또는 커스텀 드래그 제스처
- 콘텐츠: SwiftUI 뷰를 `NSHostingView`로 패널에 탑재
- 타이머: `Timer` / `Combine`, 1초 틱, 백그라운드 정확도 보정(목표 시각 기준 계산)
- 저장: `UserDefaults` (창 위치, 표시 모드, 마지막 타이머 설정)
- 알림: `NSSound` + 위젯 내 시각 효과 (+선택적 `UNUserNotification`)

---

## 개발 로드맵

| 단계 | 내용 | 결과물 |
|---|---|---|
| 0 | Xcode 프로젝트 스캐폴드 (메뉴바 앱, 패널 띄우기) | 빈 투명 창이 화면에 뜸 |
| 1 | 플로팅 패널: 투명·드래그·위치 저장 | 끌어서 옮기는 창 |
| 2 | 표시 모드(최상단/데스크탑) 전환 | 설정에 따라 레벨 변경 |
| 3 | 타이머 코어 로직 (작업 타이머) | 시작/정지/리셋 동작 |
| 4 | 퇴근 타이머 모드 | 시각 입력 → 카운트다운 |
| 5 | 고양이 이미지 + 상태 연동 | 상태별 그림 교체 |
| 6 | 고양이 종류 선택 (스킨 모델 + 설정) | 유저가 고양이 고름 |
| 7 | 종료 알림 (소리/시각) | 0초 시 알림 |
| 8 | 설정 UI 정리, 다듬기 | 사용 가능한 1차 버전 |
| 추후 | 타이머 여러 개, 프리셋, 애니메이션, 커스텀 스킨 | 확장 |

---

## 미정 / 추후 결정
- 앱 이름 확정
- 위젯 정확한 비주얼(레이아웃, 크기, 숫자 폰트)
- 고양이 이미지 실제 소스 (직접 그림 / 외주 / AI 생성)
- 배포 방식 (개인 사용 / 공유 / 앱스토어 — 서명 이슈)
